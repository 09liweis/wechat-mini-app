<!--pages/rooms/rooms.wxml-->
<view id="rooms">
  <view class="room" wx:for="{{rooms}}" wx:key="_id" bindtap="toRoomPage" data-id="{{item._id}}">
    <text>{{item.nm}}</text>
  </view>
  <view bindtap="toRoomPage" id="add-btn" hover-class="add-hover" hover-stay-time="1500">+</view>
</view>
