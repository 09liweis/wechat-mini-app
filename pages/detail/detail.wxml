<view>
  <view wx:if="todo">
    <view>{{todo.name}}</view>
    <view>{{todo.date}}</view>
    <view>{{todo.status}}</view>
    <button bindtap="delTodo" size="mini" data-todo="{{todo}}">Delete</button>
  </view>
</view>
