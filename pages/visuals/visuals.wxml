<!--pages/visuals.wxml-->
<view class="visualPage">
  <view class="visualContainer" wx:for="{{visuals}}" wx:key="_id">
    <image mode="aspectFill" class="poster" src="{{item.poster}}"></image>
    <view class="detail">
      <text>{{item.title}}</text>
    </view>
  </view>
</view>