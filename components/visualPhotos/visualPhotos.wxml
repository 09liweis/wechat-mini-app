<view class="photos block" bindtap="openPhotos">
  <view wx:for="{{photos}}" wx:key="index" wx:if="{{item.src}}" class="photo-wrapper {{item.tp}}">
    <image class="img" mode="aspectFill" src="{{item.src}}"></image>
  </view>
</view>